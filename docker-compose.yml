services:
  playwright:
    image: mcr.microsoft.com/playwright:v1.61.1-noble
    working_dir: /app

    volumes:
      - ./:/app

    command: >
      bash -c "
      npm ci &&
      npx playwright test
      "
